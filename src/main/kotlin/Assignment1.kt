fun main(){

    //premitive arrays have the same data types
    //Assignment write an array of size 50 items of type int and traverse through the array using a for loop
  val myArray = Array<Int>(50, {2})

    for (item in myArray) {
        println(item)

    }
        fun newArray() {
            val newArray = Array<Double>(6, { 3.0 })
            newArray[0] = 9.2
            newArray[1] = 10.1
            newArray[2] = 9.5
            newArray[3] = 10.4
            newArray[4] = 9.3
            newArray[5] = 91.4
            newArray[6] = 90.4
            for (x in newArray) {
                println(x)

                fun newElements() {

                    val students = arrayOf<String>("Ivan", "Aron", "Grace", "Nisah", "Christine")
                    for (l in students) {
                        println(l)
                    }
                }
            }
        }
    }


//    //non-premitive arrays have different data types
//    val firstArray = arrayOf("Kenya", "Uganda", "Tanzania", 8, true)
//    val firstArray = arrayOf("allan",8,13,14, "doctor")

